# ═══════════════════════════════════════════════════════════
# OMEGA CLAW — Environment Configuration
# ═══════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# .env is gitignored — your real keys NEVER touch GitHub.
# ═══════════════════════════════════════════════════════════

# ─── REQUIRED ────────────────────────────────────────────
# Get this from @BotFather on Telegram (free, 60 seconds)
TELEGRAM_BOT_TOKEN=your_token_here

# Your Telegram user ID (get it from @userinfobot on Telegram)
TELEGRAM_ALLOWED_USER_IDS=your_id_here

# ─── PATHS ───────────────────────────────────────────────
# Where the Constitution Hive lives (jobs are dropped here)
OMEGA_HIVE_DIR=~/Documents/Omega Constitution Pack/Omega System Public/Constution V10/USER SPACE/dev-work/hive

# Where the SQLite database is stored
OMEGA_CLAW_DB=~/.omega-claw/omega_claw.db

# ─── ENCRYPTION ──────────────────────────────────────────
# Secret 32-byte url-safe base64-encoded string for DB encryption.
# Generate via python: `from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())`
OMEGA_CLAW_DB_KEY=your_encryption_key_here

# ─── BRIDGE MODE (Recommended) ──────────────────────────
# When true, Telegram messages go directly to Claude Code terminal
# This gives you real AI understanding + full tool access
# Python spawns Claude and bridges your messages to it
OMEGA_BRIDGE_MODE=true

# ─── AUTONOMOUS MODE ─────────────────────────────────────
# When true, Omega Claw automatically invokes Claude Code for new jobs
# Claude builds autonomously, reports progress, asks for input when blocked
OMEGA_AUTONOMOUS_MODE=false

# Invoker mode for autonomous operation:
# - simple: Basic -p mode (limited - no tool access)
# - interactive: PTY/pexpect mode (full tool access, requires: pip install pexpect)
# - script: Script generation mode (Claude generates scripts, Python executes)
# Recommended: "script" for reliability, "interactive" for full power
OMEGA_INVOKER_MODE=script

# ─── FUTURE: SKILL API KEYS ─────────────────────────────
# Add keys here as you install skills that need them.
# Skills read from env using env:VAR_NAME syntax.
# OPENAI_API_KEY=sk-...
# SUPABASE_URL=https://...
# SUPABASE_KEY=...
# VERCEL_TOKEN=...
# GITHUB_TOKEN=ghp_...

# ─── FUTURE: MCP CONNECTIONS ────────────────────────────
# MCP configs in mcps/ reference these env vars.
# SUPABASE_MCP_URL=env:SUPABASE_URL
# GITHUB_MCP_TOKEN=env:GITHUB_TOKEN
